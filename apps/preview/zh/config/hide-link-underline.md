# hideLinkUnderline

- 类型：`boolean`
- 默认值：`true`

隐藏 `.vp-doc` 内链接的文字下划线。

```ts
export default defineConfig({
  themeConfig: {
    hideLinkUnderline: false
  }
})
```

设为 `false` 可恢复 VitePress 默认链接下划线。

