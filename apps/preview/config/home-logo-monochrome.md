# homeLogoMonochrome

- Type: `boolean`
- Default: `false`

Uses the home Hero image as a monochrome mask colored with the current brand token.

```ts
export default defineConfig({
  themeConfig: {
    homeLogoMonochrome: true
  }
})
```

This setting affects the default VitePress home Hero image and works best with transparent logo artwork.

