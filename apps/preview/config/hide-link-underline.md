# hideLinkUnderline

- Type: `boolean`
- Default: `true`

Hides text underlines for links inside `.vp-doc`.

```ts
export default defineConfig({
  themeConfig: {
    hideLinkUnderline: false
  }
})
```

Set it to `false` to restore the VitePress default link underline.

